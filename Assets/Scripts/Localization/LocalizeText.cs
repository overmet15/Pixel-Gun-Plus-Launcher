using UnityEngine;
using UnityEngine.UI;

public class LocalizeText : MonoBehaviour
{
    /*public string key;
    public Text text;

    [SerializeField] private bool dontUpdateOnStart;
    private void Awake()
    {
        Localization.onLocalizationChange.AddListener(OnUpdate);
    }

    void Start()
    {
        if (!dontUpdateOnStart) OnUpdate();
    }

    private void OnUpdate()
    {
        LocalizationItem item = Localization.GetItem(key);

        text.text = item.value;
        text.fontSize = item.fontSize;
    }*/
}
